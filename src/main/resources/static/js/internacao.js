/*$(document).ready(function(){
	//
	var status = $('#status').val();
	if (status == "Ativa") {
		$('.entrada').show();
		$('.saida').hide();
	}
	if (status == "Encerrada") {
		$('.saida').show();
		$('.entrada').hide();
	}
	if(status == ""){
		$('.saida').hide();
		$('.entrada').hide();
	}
	$('#status').on('change', function() {
		var status = $(this).val();
		if (status == "Ativa") {
			$('.entrada').show();
			$('.saida').hide();
		}
		if (status == "Encerrada") {
			$('.saida').show();
			$('.entrada').hide();
		}
		if(status == ""){
			$('.saida').hide();
			$('.entrada').hide();
		}
    })
})
*/